import styled from 'styled-components'

import { Colors } from '../../assets/styles/variables'

export const AuthLayoutElement = styled.div`
	align-items: center;
	background: ${Colors.BACKGROUND};
	display: flex;
	flex-direction: column;
	justify-content: center;
	min-height: 100vh;
`
